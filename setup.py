from setuptools import setup

setup(
    name='RFExplorer',    # This is the name of your PyPI-package.
    version='1.26.1806.1',                          # Update the version number for new releases
    install_requires=[
        "pyserial",
    ],
)
